const { expect } = require('chai');

describe('Community Event Planner basic test', function () {
  it('should confirm the test environment is working', function () {
    expect(true).to.equal(true);
  });
});
